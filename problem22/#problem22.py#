
import sys

def main():
    # Import the file
    f = open("names.txt")
    line = f.read()
    names = line.split(",")
    
    # Populate the alphabet dictionary
    alphas = {}
    position = 1
    for i in xrange(65, 91):
        alphas[char(i)] = position
        position += 1

    
